
import * as conf from '../../conf'

const one = async function(vue: any) {
    // let res = await vue.net.get('book', vue.jwt(), { populate: '*' })
    // if (res) { res.data = vue.strapi.reset(res.data) }
    return true
}

export default {
    one
}